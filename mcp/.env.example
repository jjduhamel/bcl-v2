# Copy to mcp/.env and fill in. mcp/.env is gitignored.

# Private key the agent signs with. 0x-prefixed, 32 bytes.
PRIVATE_KEY=0x

# Deployed Lobby proxy address. For local dev, take this from
# broadcast/DeployLobby.s.sol/31337/run-latest.json after `npm run deploy:lobby`.
LOBBY_ADDRESS=0x

# Optional. Defaults to http://127.0.0.1:8545 (anvil).
# RPC_URL=http://127.0.0.1:8545
